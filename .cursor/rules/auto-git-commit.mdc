---
description: 改完自动 commit，并定期 push 到 origin/main
alwaysApply: true
---

# 自动 Git 提交与同步（OPS Alpha）

在本仓库完成**一组可交付的代码修改**后，**无需用户再说「提交」**，自动 commit；commit 后若 `main` 领先 `origin/main`，执行 `git push origin main`（多机协作保持 GitHub 最新）。

## 何时提交

- 用户任务已做完，或本回合内相关改动已收尾。
- 仅文档/注释微调且用户明确只要问答：可不提交。
- **无变更**（`git status` 干净）：不要空提交。

## 提交前检查

1. `git status` / `git diff`，只 stage **与本任务相关**的文件；**禁止** `git add -A`。
2. **永不提交**：`.env*`、`credentials*`、`check_changes*.js`、`test_sql.js` 及明显本地调试脚本。
3. 若改动涉及 `src/`、`package.json` 或 Next 配置：先 `npm run build`；**失败则修完再提交，不要提交坏构建**。
4. 不修改 `git config`；不用 `-i`；不 `push --force`；不 `--no-verify`；不擅自 `amend`。

## 提交步骤

```bash
git add <相关文件>
git commit -m "$(cat <<'EOF'
<type>: <一句话说明原因>

EOF
)"
git status
git push origin main   # 仅当 ahead 且 push 成功；失败则告知用户，不 force
```

- `<type>`：`feat` | `fix` | `perf` | `chore` | `docs`，与近期历史一致。
- 消息 1–2 句，写**为什么**，不写文件清单。

## 用户说「定期提交 / 同步」

- 有未提交改动：按上文 commit + push。
- 工作区已干净但领先 origin：只 `git push origin main`。
- 无任何变更且未领先：说明已同步，不要空提交。

## 部署

`deploy` / 上生产仍须用户明确要求；push 不等于部署。

## 与用户全局规则的关系

本仓库以本条为准：**改完即 commit，并 push 到 GitHub**。
